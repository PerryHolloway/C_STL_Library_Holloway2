#ifndef sort-Algorithm-h
#define sort-Algorithm-h
#include <vector>
#include <algorithm>

void sort();


void sort() {
  int arr[200];
  sort(arr,arr + 200);
  vector<int> t1;
  sort(t1.begin(),t1.end());
}

#endif